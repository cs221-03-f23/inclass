// C useful stuff

// might be useful for finding best score for X or O
MIN(a, b)
MAX(a, b)

// ternary statement
int x = a < b ? 1 : 0;

// equiv
if (a < b) {
    x = 1;
} else {
    x = 0;
}

// getting user input
int r;
int c;
fscanf(stdin, "%d %d\n", &r, &c)
